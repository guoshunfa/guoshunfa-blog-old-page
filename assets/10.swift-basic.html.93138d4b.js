import{_ as p,r as i,o as l,c as d,a as n,b as s,d as e,w as c,e as t}from"./app.4659d0e0.js";const r={},u=n("h1",{id:"swift基础-基础知识",tabindex:"-1"},[n("a",{class:"header-anchor",href:"#swift基础-基础知识","aria-hidden":"true"},"#"),s(" Swift基础 - 基础知识")],-1),k=n("p",null,"翻译自：https://docs.swift.org/swift-book/LanguageGuide/TheBasics.html",-1),m=n("p",null,"Swift 是 iOS、macOS、watchOS 和 Apple tvOS app 开发的新编程语言。尽管如此，从您在C和Objective-C开发的经验来看，Swift的许多部分都会很熟悉。",-1),v=n("code",null,"Int",-1),b=n("code",null,"Double",-1),g=n("code",null,"Float",-1),h=n("code",null,"Bool",-1),f=n("code",null,"String",-1),w=n("code",null,"Array",-1),_=n("code",null,"Set",-1),q=n("code",null,"Dictionary",-1),y={href:"https://docs.swift.org/swift-book/LanguageGuide/CollectionTypes.html",target:"_blank",rel:"noopener noreferrer"},x=t(`<p>与C一样，Swift使用变量通过标识名称存储和引用值。Swift还广泛使用值无法更改的变量。这些被称为常数，比C中的常数强大得多。在整个 Swift 中，常量会使用，使代码在处理不需要更改的值时更加安全、更清晰。</p><p>除了熟悉的类型外，Swift还引入了Objective-C中没有的高级类型，例如元组。元组使您能够创建和传递值分组。您可以使用元组从函数中返回多个值作为单个复合值。</p><p>Swift还引入了可选类型，用于处理值缺失的情况。可选内容要么说“有一个值，它等于<em>x</em>”或“<em>根本没有</em>值”。使用可选类似于在Objective-C中使用<code>nil</code>和指针，但它们适用于任何类型，而不仅仅是类。可选选项不仅比Objective-C中的<code>nil</code>指针更安全、更具表现力，而且是Swift许多最强大功能的核心。</p><p>Swift 是一种<em>类型安全的</em>语言，这意味着该语言可帮助您清楚地了解代码可以使用的值类型。如果您的部分代码需要<code>String</code>，则类型安全可以防止您错误地传递给<code>Int</code>。同样，类型安全可防止您意外地将可选<code>String</code>传递给需要非可选<code>String</code>的代码。类型安全可帮助您在开发过程中尽早捕获和修复错误。</p><h2 id="常量和变量-constants-and-variables" tabindex="-1"><a class="header-anchor" href="#常量和变量-constants-and-variables" aria-hidden="true">#</a> 常量和变量(Constants and Variables)</h2><p>常量和变量将名称（如<code>maximumNumberOfLoginAttempts</code>或<code>welcomeMessage</code>）与特定类型的值（如数字<code>10</code>或字符串<code>&quot;Hello&quot;</code>相关联。<em>常量</em>的值一旦设置就无法更改，而<em>变量</em>将来可以设置为不同的值。</p><h3 id="声明常量和变量" tabindex="-1"><a class="header-anchor" href="#声明常量和变量" aria-hidden="true">#</a> 声明常量和变量</h3><p>常量和变量在使用之前必须声明。您可以使用<code>let</code>关键字声明常量，并使用<code>var</code>关键字声明变量。以下是如何使用常量和变量来跟踪用户尝试登录次数的示例：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> maximumNumberOfLoginAttempts <span class="token operator">=</span> <span class="token number">10</span>
<span class="token keyword">var</span> currentLoginAttempt <span class="token operator">=</span> <span class="token number">0</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>此代码可以读作：</p><p>“声明一个名为<code>maximumNumberOfLoginAttempts</code>的新常量，并给它的值10。然后，声明一个名为<code>currentLoginAttempt</code>的新变量，并给它一个初始值<code>0</code>。”</p><p>在本例中，允许的最大登录尝试次数被声明为常量，因为最大值永远不会改变。当前登录尝试计数器声明为变量，因为每次登录尝试失败后，此值必须递增。</p><p>您可以在一行上声明多个常量或多个变量，用逗号分隔：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">var</span> x <span class="token operator">=</span> <span class="token number">0.0</span><span class="token punctuation">,</span> y <span class="token operator">=</span> <span class="token number">0.0</span><span class="token punctuation">,</span> z <span class="token operator">=</span> <span class="token number">0.0</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><blockquote><p>注意</p><p>如果代码中的存储值不会更改，请始终将其声明为带有<code>let</code>关键字的常量。仅使用变量来存储需要能够更改的值。</p></blockquote><h3 id="类型注释" tabindex="-1"><a class="header-anchor" href="#类型注释" aria-hidden="true">#</a> 类型注释</h3><p>您可以在声明常量或变量时提供<em>类型注释</em>，以明确常量或变量可以存储的值类型。通过在常量或变量名称后放置冒号，后跟空格，后跟要使用的类型名称来编写类型注释。</p><p>此示例为名为<code>welcomeMessage</code>的变量提供了一个类型注释，以指示该变量可以存储<code>String</code>值：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">var</span> welcomeMessage<span class="token punctuation">:</span> <span class="token class-name">String</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>声明中的冒号的意思是“类型......”，因此上面的代码可以理解为：</p><p>“Declare a variable called <code>welcomeMessage</code> that’s of type <code>String</code>.”</p><p>“<code>String</code>类型”一词的意思是“可以存储任何<code>String</code>值”。把它想象成“可以存储的东西的类型”（或“东西的类型”）。</p><p><code>welcomeMessage</code>变量现在可以设置为任何字符串值，没有错误：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code>welcomeMessage <span class="token operator">=</span> <span class="token string-literal"><span class="token string">&quot;Hello&quot;</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>您可以在一行上定义同一类型的多个相关变量，用逗号分隔，并在最终变量名称后面添加单个类型注释：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">var</span> red<span class="token punctuation">,</span> green<span class="token punctuation">,</span> blue<span class="token punctuation">:</span> <span class="token class-name">Double</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,26),S=n("p",null,"注意",-1),I={href:"https://docs.swift.org/swift-book/LanguageGuide/TheBasics.html#ID322",target:"_blank",rel:"noopener noreferrer"},N=n("code",null,"welcomeMessage",-1),T=n("code",null,"welcomeMessage",-1),C=t(`<h3 id="命名常量和变量" tabindex="-1"><a class="header-anchor" href="#命名常量和变量" aria-hidden="true">#</a> 命名常量和变量</h3><p>常量和变量名几乎可以包含任何字符，包括Unicode字符：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> π <span class="token operator">=</span> <span class="token number">3.14159</span>
<span class="token keyword">let</span> 你好 <span class="token operator">=</span> <span class="token string-literal"><span class="token string">&quot;你好世界&quot;</span></span>
<span class="token keyword">let</span> 🐶🐮 <span class="token operator">=</span> <span class="token string-literal"><span class="token string">&quot;dogcow&quot;</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>常量和变量名称不能包含空格字符、数学符号、箭头、私人使用的Unicode标量值或线条和框绘制字符。它们也不能以数字开头，尽管数字可能包含在名称的其他地方。</p><p>一旦您声明了特定类型的常量或变量，您就无法以相同名称再次声明它，也无法将其更改为存储其他类型的值。您也不能将常量更改为变量或将变量更改为常量。</p><blockquote><p>注意</p><p>如果您需要为常量或变量提供与保留的Swift关键字相同的名称，请在将关键字用作名称时用反勾（\`）包围该关键字。但是，除非您完全别无选择，否则请避免使用关键字作为名称。</p></blockquote><p>您可以将现有变量的值更改为兼容类型的另一个值。在本例中， <code>friendlyWelcome</code> 的值从 <code>&quot;Hello!&quot;</code> 改变成了 <code>&quot;Bonjour!&quot;</code>:</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token number">1</span><span class="token punctuation">.</span> <span class="token keyword">var</span> friendlyWelcome <span class="token operator">=</span> <span class="token string-literal"><span class="token string">&quot;Hello!&quot;</span></span>
<span class="token number">2</span><span class="token punctuation">.</span> friendlyWelcome <span class="token operator">=</span> <span class="token string-literal"><span class="token string">&quot;Bonjour!&quot;</span></span>
<span class="token number">3</span><span class="token punctuation">.</span> <span class="token comment">// friendlyWelcome is now &quot;Bonjour!&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>与变量不同，常量的值在设置后无法更改。编译代码时，尝试这样做将报告为错误：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token number">1</span><span class="token punctuation">.</span> <span class="token keyword">let</span> languageName <span class="token operator">=</span> <span class="token string-literal"><span class="token string">&quot;Swift&quot;</span></span>
<span class="token number">2</span><span class="token punctuation">.</span> languageName <span class="token operator">=</span> <span class="token string-literal"><span class="token string">&quot;Swift++&quot;</span></span>
<span class="token number">3</span><span class="token punctuation">.</span> <span class="token comment">// This is a compile-time error: languageName cannot be changed.</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="打印常量和变量" tabindex="-1"><a class="header-anchor" href="#打印常量和变量" aria-hidden="true">#</a> 打印常量和变量</h3><p>您可以使用<code>print(_:separator:terminator:)</code>函数打印常量或变量的当前值：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token function">print</span><span class="token punctuation">(</span>friendlyWelcome<span class="token punctuation">)</span>
<span class="token comment">// Prints &quot;Bonjour!&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div>`,13),O=n("code",null,"print(_:separator:terminator:)",-1),U=n("code",null,"print(_:separator:terminator:)",-1),A=n("code",null,"separator",-1),D=n("code",null,"terminator",-1),F=n("code",null,'print(someValue,terminator:"")',-1),B={href:"https://docs.swift.org/swift-book/LanguageGuide/Functions.html#ID169",target:"_blank",rel:"noopener noreferrer"},L=t(`<p>Swift使用<em>字符串插值</em>将常量或变量的名称作为占位符包含在较长的字符串中，并提示Swift将其替换为该常量或变量的当前值。将名称包装在括号中，并在开头括号前用反斜杠转义：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;The current value of friendlyWelcome is </span><span class="token interpolation-punctuation punctuation">\\(</span><span class="token interpolation">friendlyWelcome</span><span class="token interpolation-punctuation punctuation">)</span><span class="token string">&quot;</span></span><span class="token punctuation">)</span>
<span class="token comment">// Prints &quot;The current value of friendlyWelcome is Bonjour!&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div>`,2),P=n("p",null,"注意",-1),E={href:"https://docs.swift.org/swift-book/LanguageGuide/StringsAndCharacters.html#ID292",target:"_blank",rel:"noopener noreferrer"},j=t(`<h2 id="注释" tabindex="-1"><a class="header-anchor" href="#注释" aria-hidden="true">#</a> 注释</h2><p>使用注释在您的代码中包含不可执行的文本，作为对您自己的便条或提醒。编译代码时，Swift编译器会忽略注释。</p><p>Swift 中的注释与 C 中的注释非常相似。单行注释以两个正斜杠（<code>//</code>）开头：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token comment">// This is a comment.</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>多行注释以前斜杠后跟星号（<code>/*</code>）开始，以星号后跟前斜杠（<code>*/</code>）结束：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token comment">/* This is also a comment
but is written over multiple lines. */</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>与C中的多行注释不同，Swift中的多行注释可以嵌套在其他多行注释中。您可以通过启动多行注释块，然后在第一个块中启动第二个多行注释来编写嵌套注释。然后关闭第二个块，然后是第一个块：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token comment">/* This is the start of the first multiline comment.
/* This is the second, nested multiline comment. */
This is the end of the first multiline comment. */</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>嵌套的多行注释使您可以快速轻松地注释大块代码，即使代码已经包含多行注释。</p><h2 id="分号" tabindex="-1"><a class="header-anchor" href="#分号" aria-hidden="true">#</a> 分号(😉</h2><p>与许多其他语言不同，Swift不要求您在代码中的每个语句后编写分号（<code>;</code>），尽管如果您愿意，您可以这样做。但是，如果您想在一行上写多个单独的语句，<em>则</em>需要分号：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> cat <span class="token operator">=</span> <span class="token string-literal"><span class="token string">&quot;🐱&quot;</span></span><span class="token punctuation">;</span> <span class="token function">print</span><span class="token punctuation">(</span>cat<span class="token punctuation">)</span>
<span class="token comment">// Prints &quot;🐱&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="整数-integers" tabindex="-1"><a class="header-anchor" href="#整数-integers" aria-hidden="true">#</a> 整数(Integers)</h2><p><em>整数</em>是没有小数分量的整数，例如<code>42</code>和<code>-23</code>。整数要么是<em>有符号</em>（正数、零或负数），要么<em>无符号</em>（正数或零）。</p><p>Swift以8、16、32和64位形式提供有符号和无符号整数。这些整数遵循类似于C的命名约定，即8位无符号整数类型为<code>UInt8</code>，32位有符号整数类型为<code>Int32</code>。与 Swift 中的所有类型一样，这些整数类型都有大写名称。</p><h3 id="整数边界-integer-bounds" tabindex="-1"><a class="header-anchor" href="#整数边界-integer-bounds" aria-hidden="true">#</a> 整数边界(Integer Bounds)</h3><p>您可以访问每种整数类型的最小值和最大值及其<code>min</code>和<code>max</code>：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> minValue <span class="token operator">=</span> <span class="token class-name">UInt8</span><span class="token punctuation">.</span>min  <span class="token comment">// minValue is equal to 0, and is of type UInt8</span>
<span class="token keyword">let</span> maxValue <span class="token operator">=</span> <span class="token class-name">UInt8</span><span class="token punctuation">.</span>max  <span class="token comment">// maxValue is equal to 255, and is of type UInt8</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>这些属性的值是适当大小的数字类型（如上面示例中的<code>UInt8</code>），因此可以与相同类型的其他值一起用于表达式。</p><h3 id="int" tabindex="-1"><a class="header-anchor" href="#int" aria-hidden="true">#</a> Int</h3><p>在大多数情况下，您不需要选择特定大小的整数来在代码中使用。Swift提供了一种额外的整数类型<code>Int</code>，其大小与当前平台的原生单词大小相同：</p><ul><li>在32位平台上，<code>Int</code>与<code>Int32</code>的大小相同。</li><li>在64位平台上，<code>Int</code>与<code>Int64</code>的大小相同。</li></ul><p>除非您需要使用特定大小的整数，否则请始终使用<code>Int</code>来处理代码中的整数值。这有助于代码一致性和互操作性。即使在32位平台上，<code>Int</code>也可以存储在<code>-2,147,483,648</code>和<code>2,147,483,647</code>之间的任何值，并且对于许多整数范围来说足够大。</p><h3 id="uint" tabindex="-1"><a class="header-anchor" href="#uint" aria-hidden="true">#</a> UInt</h3><p>Swift还提供了一种无符号整数类型<code>UInt</code>，其大小与当前平台的原生单词大小相同：</p><ul><li>在32位平台上，<code>UInt</code>与<code>UInt32</code>的大小相同。</li><li>在64位平台上，<code>UInt</code>与<code>UInt64</code>的大小相同。</li></ul>`,26),G=n("p",null,"注意",-1),M=n("code",null,"UInt",-1),V=n("code",null,"Int",-1),W=n("code",null,"Int",-1),H={href:"https://docs.swift.org/swift-book/LanguageGuide/TheBasics.html#ID322",target:"_blank",rel:"noopener noreferrer"},R=t(`<h2 id="浮点数" tabindex="-1"><a class="header-anchor" href="#浮点数" aria-hidden="true">#</a> 浮点数</h2><p><em>浮点数</em>是具有小数分量的数字，如<code>3.14159</code>、<code>0.1</code>和<code>-273.15</code>。</p><p>浮点类型可以表示比整数类型更广泛的值范围，并且可以存储比<code>Int</code>中存储的要大得多或小得多的数字。Swift提供了两种有符号浮点数类型：</p><ul><li><code>Double</code>表示64位浮点数。</li><li><code>Float</code>表示32位浮点数。</li></ul><blockquote><p>注意</p><p><code>Double</code>精度至少为小数点后15位，而<code>Float</code>的精度可以小到小数点后6位。要使用的合适浮点类型取决于您需要在代码中使用的值的性质和范围。在这两种类型合适的情况下，首选<code>Double</code>。</p></blockquote><h2 id="类型安全和类型推断" tabindex="-1"><a class="header-anchor" href="#类型安全和类型推断" aria-hidden="true">#</a> 类型安全和类型推断</h2><p>Swift是一种<em>类型安全的</em>语言。类型安全语言鼓励您明确代码可以使用的值类型。如果您的部分代码需要<code>String</code>，则不能错误地将其传递给<code>Int</code>。</p><p>由于Swift是类型安全的，它在编译代码时执行*类型检查，*并将任何不匹配的类型标记为错误。这使您能够尽早在开发过程中捕获和修复错误。</p><p>类型检查可帮助您在处理不同类型的值时避免错误。然而，这并不意味着您必须指定您声明的每个常量和变量的类型。如果您没有指定所需的值类型，Swift会使用<em>类型推断</em>来计算适当的类型。类型推断使编译器在编译代码时能够自动推断特定表达式的类型，只需检查您提供的值。</p><p>由于类型推断，Swift需要的类型声明比C或Objective-C等语言少得多。常量和变量仍然是显式类型，但指定其类型的大部分工作都是为您完成的。</p><p>当您声明具有初始值的常量或变量时，类型推断特别有用。这通常通过在您声明常量或变量时为常量或变量分配<em>字面值</em>（或<em>字面值</em>）来完成。（字面值是直接出现在源代码中的值，例如以下示例中的<code>42</code>和<code>3.14159</code>。）</p><p>例如，如果您在不说明类型的情况下为新常量分配<code>42</code>的字面值，Swift推断您希望该常量为<code>Int</code>，因为您已经用一个看起来像整数的数字初始化了它：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> meaningOfLife <span class="token operator">=</span> <span class="token number">42</span>
<span class="token comment">// meaningOfLife is inferred to be of type Int</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>同样，如果您没有为浮点文字指定类型，Swift推断您想创建一个<code>Double</code>：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> pi <span class="token operator">=</span> <span class="token number">3.14159</span>
<span class="token comment">// pi is inferred to be of type Double</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Swift在推断浮点数类型时总是选择<code>Double</code>（而不是<code>Float</code>）。</p><p>如果您在表达式中组合整数和浮点文字，则将从上下文中推断出一种<code>Double</code>类型：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> anotherPi <span class="token operator">=</span> <span class="token number">3</span> <span class="token operator">+</span> <span class="token number">0.14159</span>
<span class="token comment">// anotherPi is also inferred to be of type Double</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>3</code>的字面值本身没有显式类型，因此从浮点字面值的存在中推断出适当的<code>Double</code>输出类型作为加法的一部分。</p><h2 id="数字文字" tabindex="-1"><a class="header-anchor" href="#数字文字" aria-hidden="true">#</a> 数字文字</h2><p>整数文字可以写成：</p><ul><li>没有前缀<em>的小数</em></li><li>带有<code>0b</code>前缀的<em>二进制</em>数字</li><li>一个<em>八度</em>数，前缀为<code>0o</code></li><li><em>十六进制</em>数字，前缀为<code>0x</code></li></ul><p>所有这些整数文字的十进制值为<code>17</code>：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> decimalInteger <span class="token operator">=</span> <span class="token number">17</span>
<span class="token keyword">let</span> binaryInteger <span class="token operator">=</span> <span class="token number">0b10001</span>       <span class="token comment">// 17 in binary notation</span>
<span class="token keyword">let</span> octalInteger <span class="token operator">=</span> <span class="token number">0o21</span>           <span class="token comment">// 17 in octal notation</span>
<span class="token keyword">let</span> hexadecimalInteger <span class="token operator">=</span> <span class="token number">0x11</span>     <span class="token comment">// 17 in hexadecimal notation</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>浮点文字可以是十进制（没有前缀）或十六进制（带有<code>0x</code>前缀）。它们必须在小数点的两侧始终有一个数字（或十六进制数字）。十进制浮点数也可以有一个可选的<em>指数</em>，用大写或小写<code>e</code>表示；十六进制浮点必须有一个指数，用大写或小写<code>p</code>表示。</p><p>对于指数为<code>exp</code>的小数，基数乘以10exp：</p><ul><li><code>1.25e2</code>意思是1.25 x 102，或<code>125.0</code>。</li><li><code>1.25e-2</code>意思是1.25 x 10-2，或<code>0.0125</code>。</li></ul><p>对于指数为<code>exp</code>的十六进制数字，基数乘以2exp：</p><ul><li><code>0xFp2</code>意思是15 x 22或<code>60.0</code>。</li><li><code>0xFp-2</code>意思是15 x 2-2，或<code>3.75</code>。</li></ul><p>所有这些浮点文字的十进制值为<code>12.1875</code>：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> decimalDouble <span class="token operator">=</span> <span class="token number">12.1875</span>
<span class="token keyword">let</span> exponentDouble <span class="token operator">=</span> <span class="token number">1.21875e1</span>
<span class="token keyword">let</span> hexadecimalDouble <span class="token operator">=</span> <span class="token number">0xC.3p0</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>数字文字可以包含额外的格式，使其更容易阅读。整数和浮点数都可以用额外的零填充，并可以包含下划线，以帮助提高可读性。这两种格式都不会影响文字的基本值：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> paddedDouble <span class="token operator">=</span> <span class="token number">000123.456</span>
<span class="token keyword">let</span> oneMillion <span class="token operator">=</span> <span class="token number">1_000_000</span>
<span class="token keyword">let</span> justOverOneMillion <span class="token operator">=</span> <span class="token number">1_000_000.000_000_1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="数字类型转换" tabindex="-1"><a class="header-anchor" href="#数字类型转换" aria-hidden="true">#</a> 数字类型转换</h2><p>对于代码中的所有通用整数常量和变量，即使已知它们是非负数，也请使用<code>Int</code>类型。在日常情况下使用默认整数类型意味着整数常量和变量在您的代码中立即互操作，并将与整数字面值的推断类型匹配。</p><p>仅在手头任务特别需要时使用其他整数类型，因为外部来源的数据显式大小，或用于性能、内存使用或其他必要的优化。在这些情况下，使用显式大小的类型有助于捕获任何意外值溢出，并隐式记录所用数据的性质。</p><h3 id="整数转换" tabindex="-1"><a class="header-anchor" href="#整数转换" aria-hidden="true">#</a> 整数转换</h3><p>可以存储在整数常数或变量中的数字范围因每种数字类型而异。<code>Int8</code>常量或变量可以存储<code>-128</code>和<code>127</code>之间的数字，而<code>UInt8</code>常量或变量可以存储<code>0</code>到<code>255</code>之间的数字。编译代码时，不适合大小整数类型的常量或变量的数字将报告为错误：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> cannotBeNegative<span class="token punctuation">:</span> <span class="token class-name">UInt8</span> <span class="token operator">=</span> <span class="token operator">-</span><span class="token number">1</span>
<span class="token comment">// UInt8 can&#39;t store negative numbers, and so this will report an error</span>
<span class="token keyword">let</span> tooBig<span class="token punctuation">:</span> <span class="token class-name">Int8</span> <span class="token operator">=</span> <span class="token class-name">Int8</span><span class="token punctuation">.</span>max <span class="token operator">+</span> <span class="token number">1</span>
<span class="token comment">// Int8 can&#39;t store a number larger than its maximum value,</span>
<span class="token comment">// and so this will also report an error</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>由于每种数字类型可以存储不同范围的值，因此您必须根据具体情况选择数字类型转换。这种选择加入方法可以防止隐藏的转换错误，并有助于在代码中明确类型转换意图。</p><p>要将一种特定数字类型转换为另一种特定数字类型，请使用现有值初始化所需类型的新数字。在下面的示例中，常量<code>twoThousand</code>是类型<code>UInt16</code>，而常数是<code>UInt8</code>类型。它们不能直接添加在一起，因为它们不是同一类型。相反，此示例调用<code>UInt16(one)</code>创建一个新的<code>UInt16</code>，初始化为<code>one</code>，并使用此值代替原始值：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> twoThousand<span class="token punctuation">:</span> <span class="token class-name">UInt16</span> <span class="token operator">=</span> <span class="token number">2_000</span>
<span class="token keyword">let</span> one<span class="token punctuation">:</span> <span class="token class-name">UInt8</span> <span class="token operator">=</span> <span class="token number">1</span>
<span class="token keyword">let</span> twoThousandAndOne <span class="token operator">=</span> twoThousand <span class="token operator">+</span> <span class="token class-name">UInt16</span><span class="token punctuation">(</span>one<span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>由于添加的两侧现在都是<code>UInt16</code>类型，因此允许添加。输出常数（<code>twoThousandAndOne</code>）被推断为<code>UInt16</code>类型，因为它是两个<code>UInt16</code>值的总和。</p>`,43),z=n("code",null,"SomeType(ofInitialValue)",-1),K=n("code",null,"UInt16",-1),X=n("code",null,"UInt8",-1),Y=n("code",null,"UInt8",-1),J=n("code",null,"UInt16",-1),Q=n("em",null,"任何",-1),Z=n("code",null,"UInt16",-1),$={href:"https://docs.swift.org/swift-book/LanguageGuide/Extensions.html",target:"_blank",rel:"noopener noreferrer"},nn=t(`<h3 id="整数和浮点转换" tabindex="-1"><a class="header-anchor" href="#整数和浮点转换" aria-hidden="true">#</a> 整数和浮点转换</h3><p>整数和浮点数字类型之间的转换必须明确：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> three <span class="token operator">=</span> <span class="token number">3</span>
<span class="token keyword">let</span> pointOneFourOneFiveNine <span class="token operator">=</span> <span class="token number">0.14159</span>
<span class="token keyword">let</span> pi <span class="token operator">=</span> <span class="token class-name">Double</span><span class="token punctuation">(</span>three<span class="token punctuation">)</span> <span class="token operator">+</span> pointOneFourOneFiveNine
<span class="token comment">// pi equals 3.14159, and is inferred to be of type Double</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在这里，常数<code>three</code>的值用于创建<code>Double</code>类型的新值，以便加法的两侧都是相同的类型。如果没有这种转换，将不允许添加。</p><p>浮点到整数转换也必须明确。整数类型可以用<code>Double</code>或<code>Float</code>值初始化：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> integerPi <span class="token operator">=</span> <span class="token class-name">Int</span><span class="token punctuation">(</span>pi<span class="token punctuation">)</span>
<span class="token comment">// integerPi equals 3, and is inferred to be of type Int</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>当用于以这种方式初始化新的整数值时，浮点值总是被截断。这意味着<code>4.75</code>变成4，<code>-3.9</code>变成<code>-3</code>。</p><blockquote><p>注意</p><p>组合数字常数和变量的规则与数字文字的规则不同。字面值<code>3</code>可以直接添加到字面值<code>0.14159</code>中，因为数字文字本身没有显式类型。只有在编译器评估它们时，才会推断出它们的类型。</p></blockquote><h2 id="类型别名" tabindex="-1"><a class="header-anchor" href="#类型别名" aria-hidden="true">#</a> 类型别名</h2><p><em>类型别名</em>定义了现有类型的替代名称。您可以使用<code>typealias</code>关键字定义类型别名。</p><p>当您想用上下文更合适的名称引用现有类型时，例如在处理来自外部来源的特定大小的数据时，类型别名非常有用：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">typealias</span> <span class="token class-name">AudioSample</span> <span class="token operator">=</span> <span class="token class-name">UInt16</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>定义类型别名后，您可以在可能使用原始名称的任何地方使用别名：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">var</span> maxAmplitudeFound <span class="token operator">=</span> <span class="token class-name">AudioSample</span><span class="token punctuation">.</span>min
<span class="token comment">// maxAmplitudeFound is now 0</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>在这里，<code>AudioSample</code>被定义为<code>UInt16</code>的别名。由于它是一个别名，对<code>AudioSample.min</code>的调用实际上调用<code>UInt16.min</code>，它为<code>maxAmplitudeFound</code>变量提供了初始值<code>0</code>。</p><h2 id="布尔值-booleans" tabindex="-1"><a class="header-anchor" href="#布尔值-booleans" aria-hidden="true">#</a> 布尔值(Booleans)</h2><p>Swift有一个基本的<em>布尔</em>类型，称为<code>Bool</code>。布尔值被称为<em>逻辑值</em>，因为它们只能是真或假。Swift提供了两个布尔常量值，<code>true</code>和<code>false</code>：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> orangesAreOrange <span class="token operator">=</span> <span class="token boolean">true</span>
<span class="token keyword">let</span> turnipsAreDelicious <span class="token operator">=</span> <span class="token boolean">false</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>orangesAreOrange</code> 和 <code>turnnipsaredelicious</code> 的类型被推断为<code>Bool</code>，因为它们是用布尔值初始化的。与上面的&#39; Int &#39;和&#39; Double &#39;一样，如果你在创建常量或变量时将它们设置为&#39; true &#39;或&#39; false &#39;，你就不需要将它们声明为&#39; Bool &#39;。当Swift使用其他已知类型的值初始化常量或变量时，类型推断有助于使代码更加简洁和可读。</p><p>当您使用条件语句（如<code>if</code>语句）时，布尔值特别有用：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">if</span> turnipsAreDelicious <span class="token punctuation">{</span>
    <span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;Mmm, tasty turnips!&quot;</span></span><span class="token punctuation">)</span>
<span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token punctuation">{</span>
    <span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;Eww, turnips are horrible.&quot;</span></span><span class="token punctuation">)</span>
<span class="token punctuation">}</span>
<span class="token comment">// Prints &quot;Eww, turnips are horrible.&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,21),sn=n("code",null,"if",-1),en=t(`<p>Swift的类型安全防止非布尔值被替换为<code>Bool</code>。以下示例报告编译时错误：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> i <span class="token operator">=</span> <span class="token number">1</span>
<span class="token keyword">if</span> i <span class="token punctuation">{</span>
    <span class="token comment">// this example will not compile, and will report an error</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>然而，下面的替代示例是有效的：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> i <span class="token operator">=</span> <span class="token number">1</span>
<span class="token keyword">if</span> i <span class="token operator">==</span> <span class="token number">1</span> <span class="token punctuation">{</span>
​    <span class="token comment">// this example will compile successfully</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,4),an=n("code",null,"i == 1",-1),tn=n("code",null,"Bool",-1),on=n("code",null,"i == 1",-1),cn={href:"https://docs.swift.org/swift-book/LanguageGuide/BasicOperators.html",target:"_blank",rel:"noopener noreferrer"},pn=t(`<p>与 Swift 中的其他类型安全示例一样，这种方法可以避免意外错误，并确保特定代码部分的意图始终清晰。</p><h2 id="元组" tabindex="-1"><a class="header-anchor" href="#元组" aria-hidden="true">#</a> 元组</h2><p><em>元组</em>将多个值分组为单个复合值。元组中的值可以是任何类型，不必是彼此相同的类型。</p><p>在本例中，<code>(404,&quot;NotFound&quot;)</code>是描述<em>HTTP状态代码</em>的元组。HTTP状态代码是Web服务器在请求网页时返回的特殊值。如果您请求不存在的网页，则返回<code>404NotFound</code>的状态代码。</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> http404Error <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token number">404</span><span class="token punctuation">,</span> <span class="token string-literal"><span class="token string">&quot;Not Found&quot;</span></span><span class="token punctuation">)</span>
<span class="token comment">// http404Error is of type (Int, String), and equals (404, &quot;Not Found&quot;)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>(404,&quot;NotFound&quot;)</code>元组将一个<code>Int</code>和一个<code>String</code>组合在一起，为HTTP状态代码提供两个单独的值：数字和人类可读描述。它可以被描述为“类型元组<code>(Int,String)</code>”。</p><p>您可以从任何类型的排列中创建元组，它们可以包含任意数量的不同类型。没有什么可以阻止您拥有类型元组<code>(Int,Int,Int)</code>或<code>(String,Bool)</code>或者您所需的任何其他排列。</p><p>您可以将元组的内容<em>分解</em>为单独的常量或变量，然后像往常一样访问：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> <span class="token punctuation">(</span>statusCode<span class="token punctuation">,</span> statusMessage<span class="token punctuation">)</span> <span class="token operator">=</span> http404Error
<span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;The status code is </span><span class="token interpolation-punctuation punctuation">\\(</span><span class="token interpolation">statusCode</span><span class="token interpolation-punctuation punctuation">)</span><span class="token string">&quot;</span></span><span class="token punctuation">)</span>
<span class="token comment">// Prints &quot;The status code is 404&quot;</span>
<span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;The status message is </span><span class="token interpolation-punctuation punctuation">\\(</span><span class="token interpolation">statusMessage</span><span class="token interpolation-punctuation punctuation">)</span><span class="token string">&quot;</span></span><span class="token punctuation">)</span>
<span class="token comment">// Prints &quot;The status message is Not Found&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>如果您只需要元组的一些值，请在分解元组时忽略带下划线（<code>_</code>）的元组部分：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> <span class="token punctuation">(</span>justTheStatusCode<span class="token punctuation">,</span> <span class="token omit keyword">_</span><span class="token punctuation">)</span> <span class="token operator">=</span> http404Error
<span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;The status code is </span><span class="token interpolation-punctuation punctuation">\\(</span><span class="token interpolation">justTheStatusCode</span><span class="token interpolation-punctuation punctuation">)</span><span class="token string">&quot;</span></span><span class="token punctuation">)</span>
<span class="token comment">// Prints &quot;The status code is 404&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>或者，使用从零开始的索引号访问元组中的单个元素值：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;The status code is </span><span class="token interpolation-punctuation punctuation">\\(</span><span class="token interpolation">http404Error<span class="token punctuation">.</span><span class="token number">0</span></span><span class="token interpolation-punctuation punctuation">)</span><span class="token string">&quot;</span></span><span class="token punctuation">)</span>
<span class="token comment">// Prints &quot;The status code is 404&quot;</span>
<span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;The status message is </span><span class="token interpolation-punctuation punctuation">\\(</span><span class="token interpolation">http404Error<span class="token punctuation">.</span><span class="token number">1</span></span><span class="token interpolation-punctuation punctuation">)</span><span class="token string">&quot;</span></span><span class="token punctuation">)</span>
<span class="token comment">// Prints &quot;The status message is Not Found&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>当定义元组时，您可以命名元组中的单个元素：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> http200Status <span class="token operator">=</span> <span class="token punctuation">(</span>statusCode<span class="token punctuation">:</span> <span class="token number">200</span><span class="token punctuation">,</span> description<span class="token punctuation">:</span> <span class="token string-literal"><span class="token string">&quot;OK&quot;</span></span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果您命名元组中的元素，则可以使用元素名称访问这些元素的值：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;The status code is </span><span class="token interpolation-punctuation punctuation">\\(</span><span class="token interpolation">http200Status<span class="token punctuation">.</span>statusCode</span><span class="token interpolation-punctuation punctuation">)</span><span class="token string">&quot;</span></span><span class="token punctuation">)</span>
<span class="token comment">// Prints &quot;The status code is 200&quot;</span>
<span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;The status message is </span><span class="token interpolation-punctuation punctuation">\\(</span><span class="token interpolation">http200Status<span class="token punctuation">.</span>description</span><span class="token interpolation-punctuation punctuation">)</span><span class="token string">&quot;</span></span><span class="token punctuation">)</span>
<span class="token comment">// Prints &quot;The status message is OK&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,17),ln=n("code",null,"(Int,String)",-1),dn={href:"https://docs.swift.org/swift-book/LanguageGuide/Functions.html#ID164",target:"_blank",rel:"noopener noreferrer"},rn=n("p",null,"注意",-1),un={href:"https://docs.swift.org/swift-book/LanguageGuide/ClassesAndStructures.html",target:"_blank",rel:"noopener noreferrer"},kn=t(`<h2 id="可选" tabindex="-1"><a class="header-anchor" href="#可选" aria-hidden="true">#</a> 可选</h2><p>在值可能不存在的情况下，您可以使用<em>可选选项</em>。可选代表两种可能性：要么有一个值，你可以解开可选值来访问该值，要么根本没有值。</p><blockquote><p>注意</p><p>C或Objective-C中不存在可选概念。Objective-C中最近的东西是能够从否则会返回对象的方法返回<code>nil</code>，<code>nil</code>的意思是“没有有效的对象”。然而，这仅适用于对象，不适用于结构、基本C类型或枚举值。对于这些类型，Objective-C方法通常会返回一个特殊值（如<code>NSNotFound</code>），以指示没有值。这种方法假设方法的调用者知道有一个特殊值需要测试，并记得检查它。Swift的可选选项允许您指示<em>任何类型的</em>值，而无需特殊常量。</p></blockquote><p>以下是如何使用可选选项来应对值缺失的示例。Swift的sInt类型有一个初始化器，尝试将<code>String</code>值转换为<code>Int</code>值。然而，并非每个字符串都可以转换为整数。字符串<code>&quot;123&quot;</code>可以转换为数字值<code>123</code>，但字符串<code>&quot;hello,world&quot;</code>没有明显的数字值可以转换到。</p><p>下面的示例使用初始化器尝试将<code>String</code>转换为<code>Int</code>：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> possibleNumber <span class="token operator">=</span> <span class="token string-literal"><span class="token string">&quot;123&quot;</span></span>
<span class="token keyword">let</span> convertedNumber <span class="token operator">=</span> <span class="token class-name">Int</span><span class="token punctuation">(</span>possibleNumber<span class="token punctuation">)</span>
<span class="token comment">// convertedNumber is inferred to be of type &quot;Int?&quot;, or &quot;optional Int&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>由于初始化器可能会失败，它返回一个<em>可选</em>的<code>Int</code>，而不是一个<code>Int</code>。可选的<code>Int</code>写成<code>Int?</code>，而不是<code>Int</code>。问号表示它包含的值是可选的，这意味着它可能包含<em>一些</em><code>Int</code>值，或者它可能<em>根本不</em>包含<em>任何值</em>。（它不能包含任何其他内容，例如<code>Bool</code>值或<code>String</code>值。它要么是<code>Int</code>，要么什么都不是。）</p><h3 id="nil" tabindex="-1"><a class="header-anchor" href="#nil" aria-hidden="true">#</a> nil</h3><p>您可以通过为特殊值<code>nil</code>将其设置为无值状态的可选变量：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">var</span> serverResponseCode<span class="token punctuation">:</span> <span class="token class-name">Int</span><span class="token operator">?</span> <span class="token operator">=</span> <span class="token number">404</span>
<span class="token comment">// serverResponseCode contains an actual Int value of 404</span>
serverResponseCode <span class="token operator">=</span> <span class="token nil constant">nil</span>
<span class="token comment">// serverResponseCode now contains no value</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>注意</p><p>您不能将<code>nil</code>与非可选常量和变量一起使用。如果代码中的常量或变量在某些条件下需要在没有值的情况下工作，请始终将其声明为适当类型的可选值。</p></blockquote><p>如果您在不提供默认值的情况下定义可选变量，该变量将自动设置为<code>nil</code>：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">var</span> surveyAnswer<span class="token punctuation">:</span> <span class="token class-name">String</span><span class="token operator">?</span>
<span class="token comment">// surveyAnswer is automatically set to nil</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>注意</p><p>Swift的“nil”和Objective-C中的“nil”不一样。在Objective-C中，&#39; nil &#39;是一个指向不存在对象的指针。在Swift中，&#39; nil &#39;不是一个指针——它是一个特定类型的值的缺失。任何类型的可选参数都可以设置为nil，而不仅仅是对象类型。</p></blockquote><h3 id="如果陈述和强制打开包装" tabindex="-1"><a class="header-anchor" href="#如果陈述和强制打开包装" aria-hidden="true">#</a> 如果陈述和强制打开包装</h3><p>你可以使用&#39; if &#39;语句，通过比较可选的和&#39; nil &#39;来确定可选的是否包含一个值。可以使用“等于”操作符(&#39; == &#39;)或“不等于”操作符(&#39; != &#39;)执行此比较。</p><p>如果可选选项具有值，则将其视为“不等于”<code>nil</code>：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">if</span> convertedNumber <span class="token operator">!=</span> <span class="token nil constant">nil</span> <span class="token punctuation">{</span>
   <span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;convertedNumber contains some integer value.&quot;</span></span><span class="token punctuation">)</span>
<span class="token punctuation">}</span>
<span class="token comment">// Prints &quot;convertedNumber contains some integer value.&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>一旦您确定可选内容<em>确实</em>包含一个值，您可以通过添加感叹号来访问其基础值（<code>!</code>）直到可选名称的末尾。感叹号有效地说：“我知道这个可选肯定有价值；请使用它。”这被称为<em>强制打开</em>可选值：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">if</span> convertedNumber <span class="token operator">!=</span> <span class="token nil constant">nil</span> <span class="token punctuation">{</span>
   <span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;convertedNumber has an integer value of </span><span class="token interpolation-punctuation punctuation">\\(</span><span class="token interpolation">convertedNumber<span class="token operator">!</span></span><span class="token interpolation-punctuation punctuation">)</span><span class="token string">.&quot;</span></span><span class="token punctuation">)</span>
<span class="token punctuation">}</span>
<span class="token comment">// Prints &quot;convertedNumber has an integer value of 123.&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,20),mn=n("code",null,"if",-1),vn={href:"https://docs.swift.org/swift-book/LanguageGuide/ControlFlow.html",target:"_blank",rel:"noopener noreferrer"},bn=n("blockquote",null,[n("p",null,"注意"),n("p",null,[s("尝试使用"),n("code",null,"!"),s("访问不存在的可选值会触发运行时错误。在使用之前，请务必确保可选选项包含非"),n("code",null,"nil"),s("值"),n("code",null,"!"),s("强行打开它的价值。")])],-1),gn=n("h3",{id:"可选绑定",tabindex:"-1"},[n("a",{class:"header-anchor",href:"#可选绑定","aria-hidden":"true"},"#"),s(" 可选绑定")],-1),hn=n("em",null,"可选绑定",-1),fn=t(`<p>为<code>if</code>语句编写可选绑定，如下所示：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">if</span> <span class="token keyword">let</span> constantName <span class="token operator">=</span> someOptional <span class="token punctuation">{</span>
   statements
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,2),wn={href:"https://docs.swift.org/swift-book/LanguageGuide/TheBasics.html#ID330",target:"_blank",rel:"noopener noreferrer"},_n=n("code",null,"possibleNumber",-1),qn=t(`<div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">if</span> <span class="token keyword">let</span> actualNumber <span class="token operator">=</span> <span class="token class-name">Int</span><span class="token punctuation">(</span>possibleNumber<span class="token punctuation">)</span> <span class="token punctuation">{</span>
   <span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;The string \\&quot;</span><span class="token interpolation-punctuation punctuation">\\(</span><span class="token interpolation">possibleNumber</span><span class="token interpolation-punctuation punctuation">)</span><span class="token string">\\&quot; has an integer value of </span><span class="token interpolation-punctuation punctuation">\\(</span><span class="token interpolation">actualNumber</span><span class="token interpolation-punctuation punctuation">)</span><span class="token string">&quot;</span></span><span class="token punctuation">)</span>
<span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token punctuation">{</span>
   <span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;The string \\&quot;</span><span class="token interpolation-punctuation punctuation">\\(</span><span class="token interpolation">possibleNumber</span><span class="token interpolation-punctuation punctuation">)</span><span class="token string">\\&quot; couldn&#39;t be converted to an integer&quot;</span></span><span class="token punctuation">)</span>
<span class="token punctuation">}</span>
<span class="token comment">// Prints &quot;The string &quot;123&quot; has an integer value of 123&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>此代码可以读作：</p><p>“如果<code>Int(possibleNumber)</code>返回的可选<code>Int</code>包含一个值，请将名为<code>actualNumber</code>的新常量设置为可选值。”</p><p>如果转换成功，<code>actualNumber</code>常量可以在<code>if</code>语句的第一个分支中使用。它已经用可选中包含的值初始化，因此您不使用<code>!</code>后缀以访问其值。在本例中，<code>actualNumber</code>仅用于打印转换结果。</p><p>可以使用带有可选绑定的常量和变量。如果你想在&#39; If &#39;语句的第一个分支中操作&#39; actualNumber &#39;的值，你可以写&#39; If var actualNumber &#39;代替，而包含在可选的值将作为一个变量而不是一个常量可用。</p><p>您可以根据需要在单个<code>if</code>语句中包含尽可能多的可选绑定和布尔条件，并用逗号分隔。如果可选绑定中的任何值为<code>nil</code>，或者任何布尔条件计算为<code>false</code>，则整个<code>if</code>语句的条件被视为<code>false</code>。<code>if</code>陈述等效，请按以下方式处理：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">if</span> <span class="token keyword">let</span> firstNumber <span class="token operator">=</span> <span class="token class-name">Int</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;4&quot;</span></span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token keyword">let</span> secondNumber <span class="token operator">=</span> <span class="token class-name">Int</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;42&quot;</span></span><span class="token punctuation">)</span><span class="token punctuation">,</span> firstNumber <span class="token operator">&lt;</span> secondNumber <span class="token operator">&amp;&amp;</span> secondNumber <span class="token operator">&lt;</span> <span class="token number">100</span> <span class="token punctuation">{</span>
   <span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;</span><span class="token interpolation-punctuation punctuation">\\(</span><span class="token interpolation">firstNumber</span><span class="token interpolation-punctuation punctuation">)</span><span class="token string"> &lt; </span><span class="token interpolation-punctuation punctuation">\\(</span><span class="token interpolation">secondNumber</span><span class="token interpolation-punctuation punctuation">)</span><span class="token string"> &lt; 100&quot;</span></span><span class="token punctuation">)</span>
<span class="token punctuation">}</span>
<span class="token comment">// Prints &quot;4 &lt; 42 &lt; 100&quot;</span>

<span class="token keyword">if</span> <span class="token keyword">let</span> firstNumber <span class="token operator">=</span> <span class="token class-name">Int</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;4&quot;</span></span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
   <span class="token keyword">if</span> <span class="token keyword">let</span> secondNumber <span class="token operator">=</span> <span class="token class-name">Int</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;42&quot;</span></span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
       <span class="token keyword">if</span> firstNumber <span class="token operator">&lt;</span> secondNumber <span class="token operator">&amp;&amp;</span> secondNumber <span class="token operator">&lt;</span> <span class="token number">100</span> <span class="token punctuation">{</span>
           <span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;</span><span class="token interpolation-punctuation punctuation">\\(</span><span class="token interpolation">firstNumber</span><span class="token interpolation-punctuation punctuation">)</span><span class="token string"> &lt; </span><span class="token interpolation-punctuation punctuation">\\(</span><span class="token interpolation">secondNumber</span><span class="token interpolation-punctuation punctuation">)</span><span class="token string"> &lt; 100&quot;</span></span><span class="token punctuation">)</span>
       <span class="token punctuation">}</span>
   <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
<span class="token comment">// Prints &quot;4 &lt; 42 &lt; 100</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,7),yn=n("p",null,"注意",-1),xn=n("code",null,"if",-1),Sn=n("code",null,"if",-1),In=n("code",null,"guard",-1),Nn=n("code",null,"guard",-1),Tn={href:"https://docs.swift.org/swift-book/LanguageGuide/ControlFlow.html#ID525",target:"_blank",rel:"noopener noreferrer"},Cn=t('<h3 id="隐式拆开的可选" tabindex="-1"><a class="header-anchor" href="#隐式拆开的可选" aria-hidden="true">#</a> 隐式拆开的可选</h3><p>如上所述，可选表示允许常量或变量“无值”。可选选项可以用<code>if</code>语句检查，看看是否存在值，并且可以使用可选绑定有条件地展开，以访问可选值（如果存在）。</p><p>有时，从程序的结构中可以清楚地看到，在首次设置该值后，可选值将<em>始终</em>具有该值。在这些情况下，每次访问可选值时，无需检查和打开其值，因为可以安全地假设它一直具有值。</p><p>这些类型的可选选项被定义为<em>隐式未包装的可选选项</em>。您通过放置感叹号（<code>String!</code>）来编写隐式打开的可选选项而不是问号（<code>String?</code>）在您想要选择的类型之后。使用时，不要在可选名称后放置感叹号，而是在声明选项类型后放置感叹号。</p>',4),On={href:"https://docs.swift.org/swift-book/LanguageGuide/AutomaticReferenceCounting.html#ID55",target:"_blank",rel:"noopener noreferrer"},Un=t(`<p>隐式展开的可选选项是幕后正常的可选选项，但也可以像非可选值一样使用，而无需每次访问时解开可选值。以下示例显示了可选字符串和隐式未包装的可选字符串在以显式<code>String</code>的形式访问其包装值时的行为差异：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> possibleString<span class="token punctuation">:</span> <span class="token class-name">String</span><span class="token operator">?</span> <span class="token operator">=</span> <span class="token string-literal"><span class="token string">&quot;An optional string.&quot;</span></span>
<span class="token keyword">let</span> forcedString<span class="token punctuation">:</span> <span class="token class-name">String</span> <span class="token operator">=</span> possibleString<span class="token operator">!</span> <span class="token comment">// 需要一个感叹号</span>

<span class="token keyword">let</span> assumedString<span class="token punctuation">:</span> <span class="token class-name">String</span><span class="token operator">!</span> <span class="token operator">=</span> <span class="token string-literal"><span class="token string">&quot;An implicitly unwrapped optional string.&quot;</span></span>
<span class="token keyword">let</span> implicitString<span class="token punctuation">:</span> <span class="token class-name">String</span> <span class="token operator">=</span> assumedString <span class="token comment">// 不需要感叹号</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>您可以将隐式解封的可选选项视为在需要时强制打开可选选项。当您使用隐式展开的可选值时，Swift首先尝试将其用作普通的可选值；如果不能用作可选值，Swift将强制解开该值。在上面的代码中，可选值 <code>assumedString</code>在将其值分配给<code>implicitString</code>之前被强制解开，因为<code>implicitString</code>具有显式、非可选类型的<code>String</code>。在下面的代码中，<code>optionalString</code>没有显式类型，因此它是普通的可选。</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> optionalString <span class="token operator">=</span> assumedString
<span class="token comment">// optionalString的类型是“String?”，而assumedString不强制解包装。</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>如果隐式解包的可选选项为<code>nil</code>，并且您尝试访问其包装值，您将触发运行时错误。结果与您在不包含值的正常可选选项后放置感叹号完全相同。</p><p>您可以检查隐式未包装的可选选项是否为<code>nil</code>，就像检查正常可选选项一样：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">if</span> assumedString <span class="token operator">!=</span> <span class="token nil constant">nil</span> <span class="token punctuation">{</span>
   <span class="token function">print</span><span class="token punctuation">(</span>assumedString<span class="token operator">!</span><span class="token punctuation">)</span>
<span class="token punctuation">}</span>
<span class="token comment">// Prints &quot;隐式解除包装的可选字符串。&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>您还可以使用带有可选绑定的隐式解包装可选选项，在单个语句中检查和解包装其值：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">if</span> <span class="token keyword">let</span> definiteString <span class="token operator">=</span> assumedString <span class="token punctuation">{</span>
   <span class="token function">print</span><span class="token punctuation">(</span>definiteString<span class="token punctuation">)</span>
<span class="token punctuation">}</span>
<span class="token comment">// Prints &quot;An implicitly unwrapped optional string.&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p>注意</p><p>当变量以后可能变成<code>nil</code>，不要使用隐式未包装的可选选项。如果您需要在变量生命周期内检查<code>nil</code>值，请始终使用正常的可选类型。</p></blockquote><h2 id="错误处理" tabindex="-1"><a class="header-anchor" href="#错误处理" aria-hidden="true">#</a> 错误处理</h2><p>您使用<em>错误处理</em>来响应程序在执行过程中可能遇到的错误条件。</p><p>与可选选项不同，后者可以使用值的存在或不存在来传达函数的成功或失败，与之相反，错误处理允许您确定失败的根本原因，并在必要时将错误传播到程序的另一部分。</p><p>当函数遇到错误条件时，它会<em>抛出</em>错误。然后，该函数的调用者可以<em>捕获</em>错误并做出适当的响应。</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">func</span> <span class="token function-definition function">canThrowAnError</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token punctuation">{</span>
   <span class="token comment">// 这个函数可能抛出错误，也可能不抛出错误</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>函数表示可以通过在其声明中包含<code>throws</code>关键字来抛出错误。当您调用可以抛出错误的函数时，您将<code>try</code>关键字之前置于表达式。</p><p>Swift会自动将错误传播到当前范围之外，直到它们由<code>catch</code>子句处理。</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">do</span> <span class="token punctuation">{</span>
   <span class="token keyword">try</span> <span class="token function">canThrowAnError</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
   <span class="token comment">// no error was thrown</span>
<span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">{</span>
   <span class="token comment">// an error was thrown</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>do</code>语句创建一个新的包含范围，允许将错误传播到一个或多个<code>catch</code>子句。</p><p>以下是如何使用错误处理来响应不同错误条件的示例：</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">func</span> <span class="token function-definition function">makeASandwich</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token punctuation">{</span>
   <span class="token comment">// ...</span>
<span class="token punctuation">}</span>

<span class="token keyword">do</span> <span class="token punctuation">{</span>
   <span class="token keyword">try</span> <span class="token function">makeASandwich</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
   <span class="token function">eatASandwich</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
<span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token class-name">SandwichError</span><span class="token punctuation">.</span>outOfCleanDishes <span class="token punctuation">{</span>
   <span class="token function">washDishes</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
<span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token class-name">SandwichError</span><span class="token punctuation">.</span><span class="token function">missingIngredients</span><span class="token punctuation">(</span><span class="token keyword">let</span> ingredients<span class="token punctuation">)</span> <span class="token punctuation">{</span>
   <span class="token function">buyGroceries</span><span class="token punctuation">(</span>ingredients<span class="token punctuation">)</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在本例中，如果没有干净的菜肴或缺少任何成分，<code>makeASandwich()</code>函数将抛出错误。由于<code>makeASandwich()</code>可以抛出错误，函数调用被包装在<code>try</code>表达式中。通过将函数调用包装在<code>do</code>语句中，抛出的任何错误都将传播到提供的<code>catch</code>子句。</p><p>如果没有抛出错误，则调用<code>eatASandwich()</code>函数。如果抛出错误，并且与<code>SandwichError.outOfCleanDishes</code>情况匹配，则将调用<code>washDishes()</code>函数。如果抛出错误，并且它与<code>SandwichError.missingIngredients</code>的情况匹配，则使用<code>catch</code>模式捕获的关联<code>[String]</code>值调用<code>buyGroceries(_:)</code>函数。</p>`,23),An={href:"https://docs.swift.org/swift-book/LanguageGuide/ErrorHandling.html",target:"_blank",rel:"noopener noreferrer"},Dn=n("h2",{id:"断言和先决条件",tabindex:"-1"},[n("a",{class:"header-anchor",href:"#断言和先决条件","aria-hidden":"true"},"#"),s(" 断言和先决条件")],-1),Fn=n("p",null,[n("em",null,"断言"),s("和"),n("em",null,"先决条件"),s("是在运行时发生的检查。在执行任何进一步的代码之前，您可以使用它们来确保满足基本条件。如果断言或先决条件中的布尔条件计算为"),n("code",null,"true"),s("，则代码执行将照常继续。如果条件计算为"),n("code",null,"false"),s("，则程序的当前状态无效；代码执行结束，您的应用程序被终止。")],-1),Bn=n("p",null,"您使用断言和先决条件来表达您在编码时所做的假设和期望，因此您可以将它们包含在代码中。断言可帮助您在开发过程中发现错误和错误的假设，先决条件可帮助您检测生产中的问题。",-1),Ln={href:"https://docs.swift.org/swift-book/LanguageGuide/TheBasics.html#ID515",target:"_blank",rel:"noopener noreferrer"},Pn=n("p",null,"使用断言和先决条件不能替代以不太可能出现无效条件的方式设计代码。然而，使用它们来强制执行有效数据和状态会导致您的应用程序在发生无效状态时更可预测地终止，并有助于使问题更容易调试。一旦检测到无效状态就停止执行也有助于限制该无效状态造成的损害。",-1),En=n("p",null,"断言和先决条件之间的区别在于它们被检查时：断言仅在调试构建中检查，但先决条件在调试和生产构建中都被检查。在生产构建中，不评估断言中的条件。这意味着您可以在开发过程中使用任意数量的断言，而不会影响生产性能。",-1),jn=n("h3",{id:"使用断言进行调试",tabindex:"-1"},[n("a",{class:"header-anchor",href:"#使用断言进行调试","aria-hidden":"true"},"#"),s(" 使用断言进行调试")],-1),Gn={href:"https://developer.apple.com/documentation/swift/1541112-assert",target:"_blank",rel:"noopener noreferrer"},Mn=n("code",null,"assert(_:_:file:line:)",-1),Vn=n("code",null,"true",-1),Wn=n("code",null,"false",-1),Hn=n("code",null,"false",-1),Rn=t(`<div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">let</span> age <span class="token operator">=</span> <span class="token operator">-</span><span class="token number">3</span>
<span class="token function">assert</span><span class="token punctuation">(</span>age <span class="token operator">&gt;=</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token string-literal"><span class="token string">&quot;A person&#39;s age can&#39;t be less than zero.&quot;</span></span><span class="token punctuation">)</span>
<span class="token comment">// 这个断言失败，因为-3不是&gt;= 0。</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>在本例中，如果&#39; age &gt;= 0 &#39;的值为&#39; true &#39;，也就是说，如果&#39; age &#39;的值是非负的，则代码继续执行。如果&#39; age &#39;的值为负值，如上面的代码所示，那么&#39; age &gt;= 0 &#39;的计算结果为&#39; false &#39;，断言将失败，从而终止应用程序。</p><p>您可以省略断言消息——例如，当它只是作为散文重复条件时。</p><div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token function">assert</span><span class="token punctuation">(</span>age <span class="token operator">&gt;=</span> <span class="token number">0</span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,4),zn={href:"https://developer.apple.com/documentation/swift/1539616-assertionfailure",target:"_blank",rel:"noopener noreferrer"},Kn=t(`<div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token keyword">if</span> age <span class="token operator">&gt;</span> <span class="token number">10</span> <span class="token punctuation">{</span>
   <span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;You can ride the roller-coaster or the ferris wheel.&quot;</span></span><span class="token punctuation">)</span>
<span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token keyword">if</span> age <span class="token operator">&gt;=</span> <span class="token number">0</span> <span class="token punctuation">{</span>
   <span class="token function">print</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;You can ride the ferris wheel.&quot;</span></span><span class="token punctuation">)</span>
<span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token punctuation">{</span>
   <span class="token function">assertionFailure</span><span class="token punctuation">(</span><span class="token string-literal"><span class="token string">&quot;A person&#39;s age can&#39;t be less than zero.&quot;</span></span><span class="token punctuation">)</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="执行先决条件" tabindex="-1"><a class="header-anchor" href="#执行先决条件" aria-hidden="true">#</a> 执行先决条件</h3><p>每当条件可能为假时，请使用先决条件，但您的代码必须是真的才能继续执行。例如，使用先决条件检查下标没有越界，或检查函数是否传递了有效值。</p>`,3),Xn={href:"https://developer.apple.com/documentation/swift/1540960-precondition",target:"_blank",rel:"noopener noreferrer"},Yn=n("code",null,"precondition(_:_:file:line:)",-1),Jn=n("code",null,"true",-1),Qn=n("code",null,"false",-1),Zn=n("code",null,"false",-1),$n=t(`<div class="language-swift line-numbers-mode" data-ext="swift"><pre class="language-swift"><code><span class="token comment">// 在下标的实现中…</span>
<span class="token function">precondition</span><span class="token punctuation">(</span>index <span class="token operator">&gt;</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token string-literal"><span class="token string">&quot;Index must be greater than zero.&quot;</span></span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div>`,1),ns={href:"https://developer.apple.com/documentation/swift/1539374-preconditionfailure",target:"_blank",rel:"noopener noreferrer"},ss=n("code",null,"preconditionFailure(_:file:line:)",-1),es=n("blockquote",null,[n("p",null,"注意"),n("p",null,[s("如果您以未选中模式（"),n("code",null,"-Ounchecked"),s("编译，则不会检查先决条件。编译器假设先决条件始终为真，并相应地优化您的代码。然而，无论优化设置如何，thefatalError"),n("code",null,"fatalError(_:file:line:)"),s("函数总是停止执行。")]),n("p",null,[s("您可以在原型和早期开发期间使用"),n("code",null,"fatalError(_:file:line:)"),s("函数，通过编写"),n("code",null,'fatalError("Unimplemented")'),s("作为存根实现，为尚未实现的功能创建存根。由于致命错误永远不会被优化，与断言或先决条件不同，您可以确保如果遇到存根实现，执行总是会停止。")])],-1);function as(ts,os){const a=i("ExternalLinkIcon"),o=i("RouterLink");return l(),d("div",null,[u,k,m,n("p",null,[s("Swift提供了所有基本C和Objective-C类型的自己的版本，包括用于整数的"),v,s("、用于浮点值的"),b,s("和"),g,s("、用于布尔值的"),h,s("和用于文本数据的"),f,s("。Swift还提供了三种主要集合类型的强大版本，"),w,s("、"),_,s("和"),q,s("，如"),n("a",y,[s("集合类型"),e(a)]),s("中所述。")]),x,n("blockquote",null,[S,n("p",null,[s("在实践中，您很少需要编写类型注释。如果您在定义的点为常量或变量提供初始值，Swift几乎总是可以推断用于该常量或变量的类型，如"),n("a",I,[s("类型安全和类型推断中"),e(a)]),s("所述。在上面的"),N,s("示例中，没有提供初始值，因此"),T,s("变量的类型用类型注释指定，而不是从初始值推断。")])]),C,n("p",null,[O,s("函数是一个全局函数，将一个或多个值打印到适当的输出。例如，在Xcode中，"),U,s("函数在Xcode的“控制台”窗格中打印其输出。"),A,s("和"),D,s("参数具有默认值，因此您可以在调用此函数时省略它们。默认情况下，该函数通过添加换行符来终止其打印的行。要打印后没有换行符的值，请传递一个空字符串作为终止符——例如，"),F,s("。有关具有默认值的参数的信息，请参阅"),n("a",B,[s("默认参数值"),e(a)]),s("。")]),L,n("blockquote",null,[P,n("p",null,[s("可用于字符串插值的所有选项都在"),n("a",E,[s("字符串插值"),e(a)]),s("中描述。")])]),j,n("blockquote",null,[G,n("p",null,[s("仅当您特别需要与平台原生单词大小相同的无符号整数类型时，才使用"),M,s("。如果不是这样，最好是"),V,s("，即使已知要存储的值是非负值。一致地使用"),W,s("进行整数值有助于代码互操作性，避免在不同数字类型之间转换，并匹配整数类型推断，如"),n("a",H,[s("类型安全和类型推断"),e(a)]),s("所述。")])]),R,n("p",null,[z,s("是调用Swift类型的初始化器并传递初始值的默认方式。在幕后，"),K,s("有一个接受"),X,s("值的初始化器，因此此初始化器用于从现有的"),Y,s("制作新的"),J,s("。然而，您不能在这里传递"),Q,s("类型——它必须是"),Z,s("提供初始化器的类型。扩展涵盖了扩展现有类型以提供接受新类型（包括您自己的类型定义）的初始化"),n("a",$,[s("器"),e(a)]),s("。")]),nn,n("p",null,[e(o,{to:"/pages/24ff35/"},{default:c(()=>[s("Control Flow")]),_:1}),s("更详细地介绍了条件语句，如"),sn,s("语句。")]),en,n("p",null,[s("The result of the "),an,s(" comparison is of type "),tn,s(", and so this second example passes the type-check. Comparisons like "),on,s(" are discussed in "),n("a",cn,[s("Basic Operators"),e(a)]),s(".")]),pn,n("p",null,[s("元组作为函数的返回值特别有用。尝试检索网页的函数可能会返回"),ln,s("元组类型，以描述页面检索的成功或失败。通过返回具有两个不同值的元组，每个值都具有不同的类型，该函数提供了有关其结果的更有用的信息，而不是只能返回单个类型的单个值。有关更多信息，请参阅"),n("a",dn,[s("具有多个返回值的函数"),e(a)]),s("。")]),n("blockquote",null,[rn,n("p",null,[s("元组对简单的相关值组非常有用。它们不适合创建复杂的数据结构。如果您的数据结构可能更复杂，请将其建模为类或结构，而不是元组。有关更多信息，请参阅"),n("a",un,[s("结构和类"),e(a)]),s("。")])]),kn,n("p",null,[s("有关"),mn,s("语句的更多信息，请参阅"),n("a",vn,[s("控制流"),e(a)]),s("。")]),bn,gn,n("p",null,[s("你可以使用"),hn,s("来确定一个可选绑定是否包含一个值，如果是，则将该值用作临时常量或变量。可选绑定可以与' if '和' while '语句一起使用，以检查可选绑定中的值，并将该值提取到一个常量或变量中，作为单个操作的一部分。“if”和“while”语句在"),e(o,{to:"/pages/24ff35/"},{default:c(()=>[s("Control Flow")]),_:1}),s("中有更详细的描述。")]),fn,n("p",null,[s("您可以从"),n("a",wn,[s("可选"),e(a)]),s("部分重写"),_n,s("示例，以使用可选绑定而不是强制展开包装：")]),qn,n("blockquote",null,[yn,n("p",null,[s("在"),xn,s("语句中使用可选绑定创建的常量和变量仅在"),Sn,s("语句的正文中可用。相比之下，使用"),In,s("语句创建的常量和变量可以在"),Nn,s("语句后面的代码行中找到，如"),n("a",Tn,[s("Early Exit"),e(a)]),s("所述。")])]),Cn,n("p",null,[s("当可选值在首次定义可选后立即确认存在时，隐式未包装的可选值非常有用，并且可以肯定可以假设在此后的每个点都存在。Swift中隐式未包装的可选选项的主要用途是在类初始化期间，如"),n("a",On,[s("Unowned References和Imlicitly Unwrapped Optional Properties"),e(a)]),s("中所述。")]),Un,n("p",null,[n("a",An,[s("错误处理"),e(a)]),s("中更详细地介绍了抛出、捕获和传播错误。")]),Dn,Fn,Bn,n("p",null,[s("除了在运行时验证您的期望外，断言和先决条件也成为代码中有用的文档形式。与上面"),n("a",Ln,[s("错误处理"),e(a)]),s("中讨论的错误条件不同，断言和先决条件不用于可恢复或预期错误。由于失败的断言或先决条件表示无效的程序状态，因此无法捕获失败的断言。")]),Pn,En,jn,n("p",null,[s("您通过从Swift标准库调用"),n("a",Gn,[Mn,e(a)]),s("函数来编写断言。您传递此函数，一个计算为"),Vn,s("或"),Wn,s("表达式，如果条件的结果为"),Hn,s("，则显示一条消息。例如：")]),Rn,n("p",null,[s("如果代码已经检查了条件，则使用"),n("a",zn,[s("' assertionFailure(:file:line:) '"),e(a)]),s("function表示断言失败。例如:")]),Kn,n("p",null,[s("您可以通过调用"),n("a",Xn,[Yn,e(a)]),s("函数编写先决条件。您传递此函数，一个计算为"),Jn,s("或"),Qn,s("表达式，如果条件的结果为"),Zn,s("，则显示一条消息。例如：")]),$n,n("p",null,[s("您还可以调用"),n("a",ns,[ss,e(a)]),s("函数来指示发生了故障——例如，如果采用了交换机的默认情况，但所有有效的输入数据都应该由交换机的其他情况之一处理。")]),es])}const cs=p(r,[["render",as],["__file","10.swift-basic.html.vue"]]);export{cs as default};
